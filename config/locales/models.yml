es:
  dictionary:
    description: &description 'Descripción'
    email: &email 'Correo'
    name: &name 'Nombre'
    filter: &filter 'Filtrar'

  activerecord:
    models:
      # Example -> property: "propiedad"
    attributes:
      user:
        name: *name
        email: *email
        password: Clave
        password_confirmation: Confirmación
        current_password: Clave Actual
        role: Rol

